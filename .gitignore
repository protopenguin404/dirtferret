# Build output (legacy, from before workspace approach)
build/

# Assembled workspace (immutable CEF + mutable sources, recreated by shellHook)
workspace/

# CEF binary distribution files (should not be in repo root — come from nix store)
Release/
Resources/
include/
libcef_dll/
cmake/
bazel/
BUILD.bazel
MODULE.bazel
WORKSPACE
CREDITS.html
LICENSE.txt
README.txt
.bazelrc
.bazelversion

# Nix result symlinks
result
result-*
